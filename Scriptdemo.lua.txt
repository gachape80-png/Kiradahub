-- Kiểm tra executor trước khi chạy script
local executor = identifyexecutor and identifyexecutor() or "Unknown"
if executor == "Fluxus" or executor == "Codex" then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Cảnh Báo",
        Text = "Fluxus và Codex không được phép sử dụng script này! Bạn đã bị kick và bị chặn IP trong 1 tiếng.",
        Duration = 10
    })
    wait(2) -- Đợi thông báo hiển thị
    game.Players.LocalPlayer:Kick("Fluxus và Codex bị cấm. Liên hệ Discord: https://discord.gg/kJ9ydA2PP4 để được hỗ trợ.")
    return -- Dừng script
end

loadstring(game:HttpGet(("https://raw.githubusercontent.com/daucobonhi/Ui-Redz-V2/refs/heads/main/UiREDzV2.lua")))()

-- Hiển thị intro khi bật script
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "Kirada VN Hack",
    Text = "Tác giả: Kirada & Habato\nNgười làm & viết: Kirada & Hiếu TV 124\nYouTube: KiradaGamer\nTikTok: HakGamer",
    Duration = 10
})

local Window = MakeWindow({
    Hub = {
        Title = "Kirada VN",
        Animation = "Youtube: Kirada VN"
    },
    Key = {
        KeySystem = false,
        Title = "Key System",
        Description = "",
        KeyLink = "",
        Keys = {"1234"},
        Notifi = {
            Notifications = true,
            CorrectKey = "Running the Script...",
            Incorrectkey = "The key is incorrect",
            CopyKeyLink = "Copied to Clipboard"
        }
    }
})

local success, err = pcall(function()
    -- Kiểm tra tải logo
    local ImageService = game:GetService("ContentProvider")
    ImageService:PreloadAsync({"rbxassetid://89326205091486"}) -- ID logo đã được thay
    print("Logo loaded successfully: rbxassetid://89326205091486")
end)
if not success then
    warn("Lỗi tải logo: " .. tostring(err) .. ". Kiểm tra ID ảnh hoặc executor.")
end

MinimizeButton({
    Image = "rbxassetid://89326205091486", -- ID logo đã được thay
    Size = {60, 60},
    Color = Color3.fromRGB(10, 10, 10),
    Corner = true,
    Stroke = false,
    StrokeColor = Color3.fromRGB(255, 0, 0)
})

------ Tab: Script Farm
local Tab1o = MakeTab({Name = "Script Farm"})

AddButton(Tab1o, {
    Name = "Redz Hub (Updated 2025)",
    Callback = function()
        local success, err = pcall(function()
            local Settings = {
                JoinTeam = "Pirates",  -- Pirates/Marines
                Translator = true  -- true/false
            }
            local scriptContent = game:HttpGet("https://pastefy.app/XMcjaaIS/raw")
            print("Redz Hub: Đã tải script, kích thước: " .. #scriptContent .. " bytes")
            loadstring(scriptContent)(Settings)
        end)
        if not success then
            warn("Lỗi load Redz Hub: " .. tostring(err))
        end
    end
})

AddButton(Tab1o, {
    Name = "H4X Script",
    Callback = function()
        local success, err = pcall(function()
            local scriptContent = game:HttpGet("https://raw.githubusercontent.com/H4xScripts/Loader/refs/heads/main/loader.lua", true)
            print("H4X Script: Đã tải script, kích thước: " .. #scriptContent .. " bytes")
            loadstring(scriptContent)()
        end)
        if not success then
            warn("Lỗi load H4X Script: " .. tostring(err))
        end
    end
})

AddButton(Tab1o, {
    Name = "Nat Hub",
    Callback = function()
        local success, err = pcall(function()
            local scriptContent = game:HttpGet("https://get.nathub.xyz/loader")
            print("Nat Hub: Đã tải script, kích thước: " .. #scriptContent .. " bytes")
            loadstring(scriptContent)()
        end)
        if not success then
            warn("Lỗi load Nat Hub: " .. tostring(err))
        end
    end
})

AddButton(Tab1o, {
    Name = "Banana Hub Fake",
    Callback = function()
        local success, err = pcall(function()
            local scriptContent = game:HttpGet("https://raw.githubusercontent.com/kimprobloxdz/Banana-Free/refs/heads/main/Protected_5609200582002947.lua.txt")
            print("Banana Hub Fake: Đã tải script, kích thước: " .. #scriptContent .. " bytes")
            loadstring(scriptContent)()
        end)
        if not success then
            warn("Lỗi load Banana Hub Fake: " .. tostring(err))
        end
    end
})

AddButton(Tab1o, {
    Name = "Quantum Hub",
    Callback = function()
        local success, err = pcall(function()
            local scriptContent = game:HttpGet("https://raw.githubusercontent.com/flazhy/QuantumOnyx/refs/heads/main/QuantumOnyx.lua")
            print("Quantum Hub: Đã tải script, kích thước: " .. #scriptContent .. " bytes")
            loadstring(scriptContent)()
        end)
        if not success then
            warn("Lỗi load Quantum Hub: " .. tostring(err))
        end
    end
})

------ Tab: 99 Đêm
local Tab2o = MakeTab({Name = "99 Đêm"})

AddButton(Tab2o, {
    Name = "NatHub",
    Callback = function()
        local success, err = pcall(function()
            local scriptContent = game:HttpGet("https://get.nathub.xyz/loader")
            print("NatHub (99 Đêm): Đã tải script, kích thước: " .. #scriptContent .. " bytes")
            loadstring(scriptContent)()
        end)
        if not success then
            warn("Lỗi load NatHub (99 Đêm): " .. tostring(err))
        end
    end
})

AddButton(Tab2o, {
    Name = "H4X",
    Callback = function()
        local success, err = pcall(function()
            local scriptContent = game:HttpGet("https://raw.githubusercontent.com/H4xScripts/Loader/refs/heads/main/loader.lua", true)
            print("H4X (99 Đêm): Đã tải script, kích thước: " .. #scriptContent .. " bytes")
            loadstring(scriptContent)()
        end)
        if not success then
            warn("Lỗi load H4X (99 Đêm): " .. tostring(err))
        end
    end
})

------ Tab: Discord
local Tab3o = MakeTab({Name = "Discord"})

AddButton(Tab3o, {
    Name = "Copy Discord Link",
    Callback = function()
        local success, err = pcall(function()
            setclipboard("https://discord.gg/kJ9ydA2PP4")
            print("Đã sao chép link Discord: https://discord.gg/kJ9ydA2PP4")
        end)
        if not success then
            warn("Lỗi sao chép link Discord: " .. tostring(err))
        else
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Discord",
                Text = "Đã sao chép link Discord vào clipboard!",
                Duration = 5
            })
        end
    end
})

AddButton(Tab3o, {
    Name = "Copy YouTube Link",
    Callback = function()
        local success, err = pcall(function()
            setclipboard("https://www.youtube.com/@kiradavn")
            print("Đã sao chép link YouTube: https://www.youtube.com/@kiradavn")
        end)
        if not success then
            warn("Lỗi sao chép link YouTube: " .. tostring(err))
        else
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "YouTube",
                Text = "Đã sao chép link YouTube vào clipboard!",
                Duration = 5
            })
        end
    end
})

AddButton(Tab3o, {
    Name = "Copy TikTok Link",
    Callback = function()
        local success, err = pcall(function()
            setclipboard("https://www.tiktok.com/@offbyebyesad")
            print("Đã sao chép link TikTok: https://www.tiktok.com/@offbyebyesad")
        end)
        if not success then
            warn("Lỗi sao chép link TikTok: " .. tostring(err))
        else
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "TikTok",
                Text = "Đã sao chép link TikTok vào clipboard!",
                Duration = 5
            })
        end
    end
})
